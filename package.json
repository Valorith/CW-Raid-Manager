{
  "name": "cw-raid-manager",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo for CW Raid Manager application (EverQuest raid management platform).",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:client",
    "dev:server": "npm --workspace server run dev",
    "dev:client": "npm --workspace client run dev",
    "build": "npm-run-all build:server build:client",
    "build:server": "npm --workspace server run build",
    "build:client": "npm --workspace client run build",
    "lint": "npm-run-all lint:server lint:client",
    "lint:server": "npm --workspace server run lint",
    "lint:client": "npm --workspace client run lint",
    "format": "npm-run-all format:server format:client"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
